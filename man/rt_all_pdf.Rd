% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rt_all.R
\name{rt_all_pdf}
\alias{rt_all_pdf}
\title{Identify and extract all transparency indicators from a PDF file.}
\usage{
rt_all_pdf(filepath)
}
\arguments{
\item{filepath}{The path to the PDF file as a string.}
}
\value{
The same one-row tibble as [rt_all()], with `article` and `pmid`
    taken from the PDF file name.
}
\description{
Converts a PDF to text with [rt_read_pdf()] and runs [rt_all()] on it, so a
    PDF can be scored in one call without writing an intermediate text file.
    Requires the poppler `pdftotext` utility.
}
\examples{
\dontrun{
pdf_path <- system.file(
  "extdata", "PMID32171256-PMC7071725.pdf", package = "rtransparency"
)
rt_all_pdf(pdf_path)
}
}
\seealso{
[rt_all()], [rt_read_pdf()], [rt_all_txt_dir()]
}
