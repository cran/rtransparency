% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rt_coi_pubmed.R
\name{rt_coi_pubmed}
\alias{rt_coi_pubmed}
\title{Fetch conflict-of-interest statements recorded in PubMed}
\usage{
rt_coi_pubmed(pmids, api_key = Sys.getenv("ENTREZ_KEY"))
}
\arguments{
\item{pmids}{A character or numeric vector of PubMed IDs.}

\item{api_key}{An NCBI API key; defaults to the `ENTREZ_KEY` environment
variable.}
}
\value{
A tibble with one row per unique PubMed ID: `pmid`,
  `has_coi_statement` and `coi_statement` (`""` when PubMed has none).
}
\description{
Retrieves the `<CoiStatement>` that PubMed records for an article, which
holds the conflict-of-interest disclosure also when it is absent from the
full-text XML (the main source of missed disclosures in the XML detector).
}
\examples{
\donttest{
# Needs internet access.
try(rt_coi_pubmed(c("32171256", "36696006")))
}
}
\seealso{
[rt_fill_coi_pubmed()], [rt_coi_pmc()]
}
